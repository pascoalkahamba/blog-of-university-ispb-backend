const mimeTypeFile = "application/pdf"; // You can use a library to determine this dynamically if needed
const mimeTypePhoto = "image/jpeg"; // You can use a library to determine this dynamically if needed

export { mimeTypeFile, mimeTypePhoto };
